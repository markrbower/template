adjustNoiseThreshold <- function( tempdf ) {
  # This should be done only once.
  # For now, range is [50,100].
  # Check the population of tempdf:
  # - increase the threshold until less than 75% of elements would be 'noise'
  # - decrease the threshold until at least 50% of elements would be 'noise'
  
}
