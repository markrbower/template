detectionAlgorithm <- function( iter_mef, persist_table) {
  library( iterators )
  library( itertools )
  
  while ( hasNext( iter_mef ) ) {
    # Load data
    
    # Find peaks
    
    # Add to graph
    
    # Find communities for those to drop
    
    # Persist communities
    
    # Drop
    
  }
  # Find communities for those that remain
  
  # Persist communities
  
  # Drop
  
  # Label signals
  
  
}

