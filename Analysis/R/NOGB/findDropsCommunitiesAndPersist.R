findDropsCommunitiesAndPersist <- function( grph, timeWindow ) {
  # Find elements in the graph that are out of the correlation-time of the most recent entries.
  
  # Find their communities.
  
  # Store them.
  
  # Delete them from the graph.
  
}
